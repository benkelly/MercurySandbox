{{- if .Values.controller.ingress.enabled }}
# Whoever reaches this can create Jobs in the namespace: set
# secrets.mercuryApiToken and put your own authentication in front.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "mercury.fullname" . }}
  labels:
    {{- include "mercury.labels" . | nindent 4 }}
  {{- with .Values.controller.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.controller.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- with .Values.controller.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.controller.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "mercury.fullname" $ }}
                port:
                  name: http
          {{- end }}
    {{- end }}
{{- end }}
